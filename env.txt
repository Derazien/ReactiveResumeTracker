# =================================================================
# REACTIVE RESUME TRACKER - ENVIRONMENT CONFIGURATION
# =================================================================
# Copy this file to .env (in PROJECT ROOT) and fill in your actual values
# This file is safe to commit - never commit .env with real secrets!

# =================================================================
# 📍 FILE LOCATIONS AFTER REORGANIZATION
# =================================================================
# .env file location: PROJECT_ROOT/.env (this directory)
# Database location: PROJECT_ROOT/apps/server/prisma/dev.db
# Prisma schema: PROJECT_ROOT/apps/server/prisma/schema.prisma
# Migrations: PROJECT_ROOT/apps/server/prisma/migrations/

# =================================================================
# ⚠️  REQUIRED VARIABLES (Must be set for server to start)
# =================================================================

# 🌍 Basic Configuration (REQUIRED)
NODE_ENV=development
PORT=3000
PUBLIC_URL=http://localhost:3000
STORAGE_URL=http://localhost:9000

# 🗄️ Database Configuration (REQUIRED)
# SQLite Database (Development) - CORRECTED PATH
DATABASE_URL=file:./apps/server/prisma/dev.db

# 🔐 Authentication & Security (REQUIRED)
# Generate strong random strings: openssl rand -base64 32
ACCESS_TOKEN_SECRET=your-super-secret-access-token-here
REFRESH_TOKEN_SECRET=your-super-secret-refresh-token-here

# 🖨️ PDF Generation (REQUIRED)
CHROME_TOKEN=your-chrome-token-here
CHROME_URL=ws://localhost:8080

# 💾 File Storage (REQUIRED)
STORAGE_ENDPOINT=localhost
STORAGE_PORT=9000
STORAGE_REGION=us-east-1
STORAGE_BUCKET=reactive-resume
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin
STORAGE_USE_SSL=false
STORAGE_SKIP_BUCKET_CHECK=false

# =================================================================
# 🎯 LLM INTEGRATION (Optional but recommended for AI features)
# =================================================================
# Choose your provider: anthropic | openai | local
LLM_PROVIDER=anthropic

# Anthropic Claude (Recommended for best performance)
ANTHROPIC_API_KEY=your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# OpenAI GPT (Alternative option)
OPENAI_API_KEY=your-openai-api-key-here  
OPENAI_MODEL=gpt-4-turbo-preview

# Local LLM (Ollama/LM Studio - Future use)
LOCAL_LLM_BASE_URL=http://localhost:11434
LOCAL_LLM_API_KEY=
LOCAL_LLM_MODEL=llama3:8b

# =================================================================
# 📧 EMAIL CONFIGURATION (Optional)
# =================================================================
MAIL_FROM=noreply@yourdomain.com
# Example: smtps://username:password@smtp.gmail.com:465
SMTP_URL=smtps://username:password@smtp.gmail.com:465

# =================================================================
# 🔗 OAUTH PROVIDERS (Optional)
# =================================================================
# GitHub OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_CALLBACK_URL=http://localhost:3000/api/auth/github/callback

# Google OAuth  
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# OpenID (Optional)
# OPENID_NAME=
# OPENID_AUTHORIZATION_URL=
OPENID_CALLBACK_URL=http://localhost:3000/api/auth/openid/callback
# OPENID_CLIENT_ID=
# OPENID_CLIENT_SECRET=
# OPENID_ISSUER=
OPENID_SCOPE=openid profile email
# OPENID_TOKEN_URL=
# OPENID_USER_INFO_URL=

# =================================================================
# 🚧 FEATURE FLAGS (Optional)
# =================================================================
DISABLE_SIGNUPS=false
DISABLE_EMAIL_AUTH=false

# =================================================================
# ☁️ DEVELOPMENT SERVICES (Optional)
# =================================================================
# Nx Cloud (Optional)
NX_CLOUD_ACCESS_TOKEN=

# Crowdin (Optional)
CROWDIN_PROJECT_ID=
CROWDIN_PERSONAL_TOKEN=

# =================================================================
# 🚀 MINIMAL .ENV TEMPLATE FOR QUICK SETUP
# =================================================================
# Copy the section below to create a working .env file:
#
# NODE_ENV=development
# PORT=3000
# PUBLIC_URL=http://localhost:3000
# STORAGE_URL=http://localhost:9000
# DATABASE_URL=file:./apps/server/prisma/dev.db
# ACCESS_TOKEN_SECRET=development-access-token-secret-change-in-production
# REFRESH_TOKEN_SECRET=development-refresh-token-secret-change-in-production
# CHROME_TOKEN=development-chrome-token
# CHROME_URL=ws://localhost:8080
# STORAGE_ENDPOINT=localhost
# STORAGE_PORT=9000
# STORAGE_REGION=us-east-1
# STORAGE_BUCKET=reactive-resume
# STORAGE_ACCESS_KEY=minioadmin
# STORAGE_SECRET_KEY=minioadmin
# STORAGE_USE_SSL=false
# STORAGE_SKIP_BUCKET_CHECK=false
# LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=your-anthropic-api-key-here
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# =================================================================
# 🔧 QUICK START GUIDE
# =================================================================
# 1. Copy this file: cp env.txt .env
# 2. Update the minimal template above with your values
# 3. For development, you can use the default values
# 4. For production, generate proper secrets:
#    openssl rand -base64 32  # for ACCESS_TOKEN_SECRET
#    openssl rand -base64 32  # for REFRESH_TOKEN_SECRET
#    openssl rand -hex 32     # for CHROME_TOKEN
# 5. Add your LLM API key (Anthropic Claude recommended)
# 6. Start development: pnpm dev

# =================================================================
# 📁 REORGANIZED PROJECT STRUCTURE
# =================================================================
# ✅ FIXED LOCATIONS:
# - Database: apps/server/prisma/dev.db (single file)
# - Schema: apps/server/prisma/schema.prisma
# - Migrations: apps/server/prisma/migrations/
# - Environment: .env (project root)
# 
# ❌ REMOVED DUPLICATES:
# - tools/prisma/ (entire directory removed)
# - tools/dev.db (empty file removed)
# - Multiple nested prisma directories cleaned up
#
# 🔧 CONFIGURATION UPDATES:
# - package.json: Updated prisma.schema path
# - Both setup scripts: Updated database paths
# - All import scripts: Work with new structure

# =================================================================
# ⚠️ IMPORTANT: ENVIRONMENT VARIABLE LOADING FIX
# =================================================================
# If you get OpenID URL validation errors, use this minimal .env template:

# Minimal working .env for ReactiveResumeTracker
NODE_ENV=development
PORT=3000
PUBLIC_URL=http://localhost:3000
STORAGE_URL=http://localhost:9000
DATABASE_URL=file:./apps/server/prisma/dev.db
ACCESS_TOKEN_SECRET=development-access-token-secret-change-in-production-please
REFRESH_TOKEN_SECRET=development-refresh-token-secret-change-in-production-please
CHROME_TOKEN=development-chrome-token-change-in-production-please
CHROME_URL=ws://localhost:8080
STORAGE_ENDPOINT=localhost
STORAGE_PORT=9000
STORAGE_REGION=us-east-1
STORAGE_BUCKET=reactive-resume
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin
STORAGE_USE_SSL=false
STORAGE_SKIP_BUCKET_CHECK=false
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
# OpenID fields commented out to prevent validation errors
# OPENID_NAME=
# OPENID_AUTHORIZATION_URL=
OPENID_CALLBACK_URL=http://localhost:3000/api/auth/openid/callback
# OPENID_CLIENT_ID=
# OPENID_CLIENT_SECRET=
# OPENID_ISSUER=
OPENID_SCOPE=openid profile email
# OPENID_TOKEN_URL=
# OPENID_USER_INFO_URL=

# =================================================================
# 🚀 HOW TO START THE APPLICATION
# =================================================================
# 
# OPTION 1: Manual Environment Variable (PowerShell)
# $env:DATABASE_URL="file:./apps/server/prisma/dev.db"; pnpm dev
#
# OPTION 2: Use the setup script
# ./setup.ps1
#
# OPTION 3: Set environment variable permanently
# [Environment]::SetEnvironmentVariable("DATABASE_URL", "file:./apps/server/prisma/dev.db", "User")
